import { Control } from './control';
export declare class ControlCircle extends Control {
    private static circleRadius;
    /**
    * Constructs a control at the position (x,y)
    */
    constructor(x: number, y: number);
}
