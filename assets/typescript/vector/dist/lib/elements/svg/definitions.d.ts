import { Marker } from '../../index';
import { Element } from './element';
export declare class Definitions extends Element {
    constructor();
    marker(refX: number, refY: number, width: number, height: number): Marker;
}
