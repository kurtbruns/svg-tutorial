import { Use } from '../svg/use';
export declare class Icon extends Use {
    /**
    * Construct an icon element at the position (x,y) with the provided dimensions
    */
    constructor(x: number, y: number, width: number, height: number);
}
